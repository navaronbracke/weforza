/// This enum declares the different filter options.
/// [MemberFilterOption.all] No filter.
/// [MemberFilterOption.active] Only active members.
/// [MemberFilterOption.inactive] Only inactive members.
enum MemberFilterOption {
  all,
  active,
  inactive,
}
